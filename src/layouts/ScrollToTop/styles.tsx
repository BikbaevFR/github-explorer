import tw from "twin.macro";

const Button = tw.button`fixed right-0 bottom-0 flex flex-col items-center transition-colors z-10 hover:bg-slate-300 rounded-tl-[100px] p-4 pr-3 pb-3 w-40 h-24`;

const IconContainer = tw.div`w-10 h-10`;

export { Button, IconContainer };
