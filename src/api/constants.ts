export const REPOS_PER_PAGE: number = 10;
export const SEARCH_PER_PAGE: number = 18;

export enum FetchStatus {
  IDLE = "IDLE",
  LOADING = "LOADING",
  SUCCEEDED = "SUCCEEDED",
  FAILED = "FAILED",
}
